version: '3.8'

services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python -m pytest tests/ -v --tb=short

